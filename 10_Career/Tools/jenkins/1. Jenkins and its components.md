```mermaid
graph TD

%% SCM Input
subgraph SCM [Version Control SCM]
    Git["Git Repository SCM<br>(Houses App Code & Jenkinsfile)"]
end

%% Jenkins Orchestration Node (Controller)
subgraph ControllerNode ["Jenkins Controller (The Brain)"]
    Controller["Jenkins Controller Server<br>(Scheduler & Orchestrator)"]
    WebUI["Web UI<br>(Port 8080)"]
    Queue["Build Queue"]
    Creds["Credentials Store"]
    JCasC["JCasC<br>(Configuration as Code)"]
end

%% Ephemeral Compute Platform (Agents)
subgraph ComputePlatform ["Distributed Compute Platform (Build Agents)"]
    K8sCloud["Kubernetes Cluster / Cloud"]
    
    subgraph AgentPod ["Ephemeral Agent Pod (On-Demand Compute)"]
        JNLP["JNLP Agent Container<br>(Inbound Remoting Execution)"]
        
        subgraph ToolContainers ["Custom Sidecar Tool Containers"]
            NodeJS["Node.js/Yarn Container"]
            Maven["Maven/Java Container"]
            DockerCLI["Docker Client Container"]
        end
    end
end

%% Security & Quality Gates
subgraph QualitySec ["Security & Quality Control Gates"]
    Vault["HashiCorp Vault<br>(Central Secrets Engine)"]
    Sonar["SonarQube Server<br>(Port 9000)"]
    DeviceFarm["Cloud Device Farm<br>(e.g., BrowserStack)"]
end

%% Relationships and Protocols
Git -->|"1. Commit / PR Trigger Webhook"| Controller
Controller -.->|"Configured via"| JCasC
Controller -->|"2. Queue Job Execution"| Queue
Queue -->|"3. Provision Ephemeral Pod"| K8sCloud
K8sCloud -->|"4. Spin up Pod"| AgentPod

%% Inbound Connection (Fixed parsing issue by quoting the label)
JNLP -->|"5. Remoting / WebSocket Link<br>(Inbound Port 50000 Connection)"| Controller

%% Executor Actions
NodeJS -->|"6a. Authenticate & Fetch Secrets<br>(AppRole / Kubernetes Token)"| Vault
Maven -->|"6b. Upload SAST Scan"| Sonar
DockerCLI -->|"6c. Offload Mobile UI Tests"| DeviceFarm

%% Handshakes
Sonar -->|"7. Quality Gate Webhook Handshake<br>(/sonarqube-webhook/)"| Controller

%% Styling
style ControllerNode fill:#f9f,stroke:#333,stroke-width:2px
style ComputePlatform fill:#bbf,stroke:#333,stroke-width:2px
style QualitySec fill:#bfb,stroke:#333,stroke-width:2px
```


![[Human Error Impact Analysis-2026-07-30-061126.png]]

- **Jenkins Server:** The central orchestrator ("city center") that schedules tasks, manages the CI/CD process, and provides the user interface.
    
- **Plugins:** The largest part of the ecosystem (over 1,000 available). They extend Jenkins' core capabilities and allow it to integrate with external services (like Source Control and Build Tools).
    
- **Nodes:** The physical or virtual "workhorses" that actually execute the tasks assigned by the server, enabling concurrent and distributed workflows.
    
- **Jobs:** The fundamental units of work, defining specific steps Jenkins needs to perform (e.g., pulling code, running tests).
    
- **Pipelines:** A specialized job type that defines the entire software delivery process as version-controlled code (usually in a `Jenkinsfile`).
    
- **Workspaces:** The dedicated directories on nodes where a job is executed, storing checked-out source code and generated build files.
```mermaid
graph TD
    subgraph External Environment
        SCM["Source Control <br/> e.g., Git, GitHub"]
        BT["Build Tools <br/> e.g., Maven, npm"]
    end

    subgraph The Jenkins Ecosystem
        JS(("Jenkins Server <br/> The City Center"))
        
        P["Plugins <br/> Services & Utilities"]
        I["Integrations <br/> Roads & Bridges"]
        
        J["Jobs <br/> Basic Unit of Work"]
        PL["Pipelines <br/> Process as Code"]
        
        N["Nodes <br/> The Workhorses"]
        W["Workspaces <br/> Execution Directories"]

        %% Core relationships
        JS -->|"Extends core functionality"| P
        JS -->|"Connects to outside world"| I
        JS -->|"Schedules & Orchestrates"| J
        JS -->|"Schedules & Orchestrates"| PL
        JS -->|"Distributes tasks to"| N

        %% Execution relationships
        J -.->|"Execute on"| N
        PL -.->|"Execute on"| N
        N -->|"Provides/Contains"| W
    end

    %% External connections
    P -->|"Interact with"| SCM
    P -->|"Interact with"| BT
    I -->|"Connect to"| SCM
    I -->|"Connect to"| BT
```
